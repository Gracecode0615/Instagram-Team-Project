const NoAccount = ({signUpColor}) => {
    return (
        <div>
            <p>Don't have an account? <span
            style={{color:signUpColor}}>Sign up</span></p>
        </div>
    );
}
 
export default NoAccount;